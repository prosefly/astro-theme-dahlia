---
interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  headings: Heading[];
}

const { headings } = Astro.props;
const t = Astro.locals.t;
const visibleHeadings = headings;

function getHeadingIndent(depth: number): string {
  if (depth <= 2) {
    return 'ps-3';
  }

  if (depth === 3) {
    return 'ps-5';
  }

  return 'ps-7';
}
---

{
  visibleHeadings.length > 0 && (
    <nav aria-label={t('tableOfContents.onThisPage')} class="space-y-2">
      <p class="text-sm font-semibold leading-5 text-(--dahlia-text-strong)">{t('tableOfContents.onThisPage')}</p>
      <ul class="dahlia-border-subtle dahlia-toc-list relative space-y-0.5 border-s" data-toc-list>
        <li aria-hidden="true" class="dahlia-toc-indicator"></li>
        {visibleHeadings.map((heading) => (
          <li>
            <a
              class:list={[
                'dahlia-focus-ring dahlia-toc-link block py-1 pe-2 text-sm leading-5 transition-colors',
                getHeadingIndent(heading.depth),
                'text-(--dahlia-text-muted) hover:text-(--dahlia-text-strong)',
              ]}
              data-depth={heading.depth}
              data-toc-link={heading.slug}
              href={`#${heading.slug}`}
            >
              {heading.text}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  )
}

<script>
  import '../../client/toc';
</script>
