---
interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const {
  as: Tag = 'div',
  class: className,
} = Astro.props as Props;
---

<Tag class:list={['dahlia-prose', className]}>
  <slot />
</Tag>
