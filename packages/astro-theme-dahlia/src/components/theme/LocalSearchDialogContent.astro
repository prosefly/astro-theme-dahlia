---
import rawThemeConfig from 'virtual:prosefly/dahlia/config';
import { getLocalizedSearchHref } from '../../lib/i18n';
import { getPagefindOutputSubdir } from '../../lib/search/index';
import type { DahliaThemeConfig } from '../../lib/theme';

const themeConfig = rawThemeConfig as DahliaThemeConfig;
const t = Astro.locals.t;
const currentLocale = t.locale();
const searchConfig = themeConfig.search === false ? undefined : themeConfig.search;
const searchProvider = searchConfig?.provider ?? 'local';
const pagefindConfig = searchConfig?.provider === 'pagefind' ? searchConfig : undefined;
const algoliaConfig = searchConfig?.provider === 'algolia' ? searchConfig : undefined;
const searchIndexHref = searchProvider === 'local'
  ? getLocalizedSearchHref(themeConfig, currentLocale.key)
  : undefined;
const pagefindBundlePath = pagefindConfig
  ? `/${getPagefindOutputSubdir(pagefindConfig)}/pagefind.js`
  : undefined;
const algoliaIndexName = algoliaConfig
  ? typeof algoliaConfig.indexName === 'string'
    ? algoliaConfig.indexName
    : algoliaConfig.indexName[currentLocale.key] ?? algoliaConfig.indexName[currentLocale.lang]
  : undefined;
const algoliaAppId = algoliaConfig?.appId;
const algoliaApiKey = algoliaConfig?.apiKey;
---

{searchConfig && (
  <dialog
    aria-label={t('search.label')}
    class="dahlia-search-dialog dahlia-border-muted w-[min(42rem,calc(100vw-2rem))] rounded-[min(var(--dahlia-radius-lg),1rem)] border bg-(--dahlia-background) p-0 text-(--dahlia-text) shadow-xl shadow-black/10"
    data-dahlia-algolia-api-key={algoliaApiKey}
    data-dahlia-algolia-app-id={algoliaAppId}
    data-dahlia-algolia-index-name={algoliaIndexName}
    data-dahlia-search-dialog
    data-dahlia-search-index={searchIndexHref}
    data-dahlia-search-loading={t('search.loading')}
    data-dahlia-search-no-results={t('search.noResults')}
    data-dahlia-pagefind-bundle={pagefindBundlePath}
    data-dahlia-search-provider={searchProvider}
    data-dahlia-search-type-to-search={t('search.typeToSearch')}
    data-dahlia-search-unavailable={t('search.unavailable')}
    id="dahlia-search-dialog"
  >
  <form class="dahlia-border-subtle flex h-14 items-center gap-3 border-b px-4" data-dahlia-search-form>
    <svg aria-hidden="true" class="h-4 w-4 shrink-0 text-(--dahlia-text-muted)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
      <circle cx="11" cy="11" r="7" />
      <path d="m20 20-3.5-3.5" />
    </svg>
    <input
      aria-autocomplete="list"
      aria-controls="dahlia-search-results"
      aria-expanded="false"
      aria-label={t('search.label')}
      class="min-w-0 flex-1 bg-transparent text-base outline-none placeholder:text-(--dahlia-text-muted)"
      autocomplete="off"
      data-dahlia-search-input
      placeholder={t('search.placeholder')}
      role="combobox"
      type="search"
    />
    <button
      aria-label={t('search.closeLabel')}
      class="dahlia-focus-ring inline-flex h-8 w-8 items-center justify-center rounded-(--dahlia-radius-md) text-(--dahlia-text-muted) transition-colors hover:text-(--dahlia-text-strong)"
      data-dahlia-search-close
      type="button"
    >
      <svg aria-hidden="true" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
        <path d="M18 6 6 18" />
        <path d="m6 6 12 12" />
      </svg>
    </button>
  </form>
  <div
    class="dahlia-scrollbar max-h-[min(30rem,calc(100dvh-10rem))] overflow-y-auto p-2"
    data-dahlia-search-results
    id="dahlia-search-results"
    aria-label={t('search.label')}
    aria-live="polite"
    role="listbox"
  >
    <div class="px-3 py-8 text-center text-sm text-(--dahlia-text-muted)">
      {t('search.typeToSearch')}
    </div>
  </div>
  </dialog>
)}

<script>
  import '../../client/search/dialog';
</script>
