---
import ThemeModeController from './ThemeModeController.astro';

const t = Astro.locals.t;
const modes = [
  { value: 'system', label: t('themeSelect.auto') },
  { value: 'light', label: t('themeSelect.light') },
  { value: 'dark', label: t('themeSelect.dark') },
];
---

<div
  class="relative"
  data-theme-switch
  data-theme-label-dark={t('themeSelect.dark')}
  data-theme-label-light={t('themeSelect.light')}
  data-theme-label-system={t('themeSelect.auto')}
>
  <select
    aria-label={t('themeSelect.accessibleLabel')}
    class="dahlia-focus-ring dahlia-border-muted h-9 rounded-(--dahlia-radius-md) border bg-(--dahlia-background) px-3 text-sm font-medium text-(--dahlia-text-strong) sm:hidden"
    data-theme-select
  >
    {modes.map((mode) => (
      <option value={mode.value}>{mode.label}</option>
    ))}
  </select>

  <details class="group/theme-select relative hidden sm:block">
    <summary class="dahlia-focus-ring dahlia-border-muted flex h-9 cursor-pointer items-center gap-2 rounded-(--dahlia-radius-md) border px-3 text-sm font-medium text-(--dahlia-text-strong) marker:hidden">
      <span data-theme-current>{t('themeSelect.auto')}</span>
      <svg aria-hidden="true" class="h-3.5 w-3.5 text-(--dahlia-text-muted) transition-transform group-open/theme-select:rotate-180" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
        <path d="m6 9 6 6 6-6" />
      </svg>
    </summary>
    <div class="dahlia-border-muted absolute end-0 top-[calc(100%+0.5rem)] z-50 min-w-32 rounded-[min(var(--dahlia-radius-lg),1rem)] border bg-(--dahlia-background) p-1 shadow-lg shadow-black/5">
      {modes.map((mode) => (
        <button
          class="dahlia-focus-ring flex w-full items-center rounded-[calc(var(--dahlia-radius-md)-2px)] px-2.5 py-1.5 text-start text-sm text-(--dahlia-text-muted) transition-colors hover:text-(--dahlia-text-strong)"
          data-theme-mode={mode.value}
          type="button"
        >
          {mode.label}
        </button>
      ))}
    </div>
  </details>
</div>

<ThemeModeController />
