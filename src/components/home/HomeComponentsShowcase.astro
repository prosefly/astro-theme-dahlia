---
import { Callout, Icon, TabItem, Tabs } from '@prosefly/astro-components';
import { PageSection } from '@prosefly/astro-theme-dahlia/layouts';
---

<PageSection class="border-b border-(--dahlia-border-subtle) bg-(--dahlia-surface)/55" padding="lg" size="xl">
  <div class="showcase-grid grid min-w-0 gap-12 lg:grid-cols-[minmax(0,0.75fr)_minmax(30rem,1.25fr)] lg:items-center">
    <div class="min-w-0">
      <p class="section-label">MDX components</p>
      <h2 class="heading mt-3">Use real UI components inside Markdown.</h2>
      <p class="mt-5 text-lg leading-8 text-(--dahlia-text-muted)">
        Import components from <code class="break-all font-mono text-base text-(--dahlia-text-strong)">@prosefly/astro-components</code>.
        Install the package directly when you use them in your own MDX.
      </p>
      <ul class="spec-list mt-7">
        {['Callouts for important context', 'Synced package-manager tabs', 'Steps, cards, accordions, file trees, badges, and icons'].map((item) => (
          <li>
            <Icon name="lucide:check" /> {item}
          </li>
        ))}
      </ul>
      <a class="text-link dahlia-focus-ring mt-8" href="https://prosefly.dev/docs/components/" rel="noreferrer" target="_blank">
        Browse component docs <Icon name="lucide:arrow-up-right" />
      </a>
    </div>

    <div class="component-stage min-w-0 max-w-full overflow-hidden border border-(--dahlia-border-subtle) bg-(--dahlia-background) p-5 sm:p-7">
      <div class="mb-5 flex flex-wrap items-center justify-between gap-2 border-b border-(--dahlia-border-subtle) pb-4">
        <span class="text-sm font-semibold text-(--dahlia-text-strong)">MDX building blocks</span>
        <span class="font-mono text-xs text-(--dahlia-text-subtle)">rendered components</span>
      </div>
      <Callout type="note" title="Shared appearance tokens">
        Components respond to the same accent, gray scale, radius, and color mode as Dahlia.
      </Callout>
      <Tabs defaultValue="pnpm" copy>
        <TabItem label="pnpm" value="pnpm">
          <pre><code>pnpm add @prosefly/astro-components</code></pre>
        </TabItem>
        <TabItem label="npm" value="npm">
          <pre><code>npm install @prosefly/astro-components</code></pre>
        </TabItem>
        <TabItem label="yarn" value="yarn">
          <pre><code>yarn add @prosefly/astro-components</code></pre>
        </TabItem>
      </Tabs>
      <div class="component-index grid border-t border-(--dahlia-border-subtle) sm:grid-cols-3">
        {['Steps', 'Cards', 'Accordions'].map((label) => (
          <div class="py-3 text-sm text-(--dahlia-text)">
            <code>{label}</code>
          </div>
        ))}
      </div>
    </div>
  </div>
</PageSection>

<style>
  .section-label {
    color: var(--dahlia-text-muted);
    font-family: var(--dahlia-font-mono);
    font-size: 0.8125rem;
  }

  .heading {
    color: var(--dahlia-text-strong);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1.08;
  }

  .text-link {
    align-items: center;
    border-radius: var(--dahlia-radius-sm);
    color: var(--dahlia-accent);
    display: inline-flex;
    font-size: 0.875rem;
    font-weight: 600;
    gap: 0.5rem;
  }

  .component-stage {
    border-radius: min(var(--dahlia-radius-lg), 1rem);
    box-sizing: border-box;
  }

  .spec-list {
    list-style: none;
    margin-bottom: 0;
    padding: 0;
  }

  .spec-list li {
    align-items: center;
    border-top: 1px solid var(--dahlia-border-subtle);
    color: var(--dahlia-text);
    display: flex;
    font-size: 0.875rem;
    gap: 0.75rem;
    padding-block: 0.75rem;
  }

  .spec-list li:last-child {
    border-bottom: 1px solid var(--dahlia-border-subtle);
  }

  .spec-list :global(.pf-icon) {
    color: var(--dahlia-accent);
    flex: 0 0 auto;
  }

  .component-index > div {
    padding-inline: 0.75rem;
  }

  .component-index > div + div {
    border-left: 1px solid var(--dahlia-border-subtle);
  }

  .component-index code {
    color: var(--dahlia-text-strong);
    font-family: var(--dahlia-font-mono);
    font-size: 0.75rem;
  }

  .component-stage :global(.pf-callout),
  .component-stage :global(.pf-tabs) {
    max-width: 100%;
    margin-block: 1rem;
    min-width: 0;
  }

  .component-stage :global(pre) {
    border: 0;
    box-sizing: border-box;
    margin: 0;
    max-width: 100%;
    overflow-x: auto;
    width: 100%;
  }

  .component-stage :global(.pf-tabs__panels),
  .component-stage :global(.pf-tabs__panel) {
    background: var(--dahlia-background);
    box-sizing: border-box;
    max-width: 100%;
    min-width: 0;
    overflow: hidden;
  }

  .component-stage :global(.pf-tabs__panel code) {
    display: block;
    min-width: max-content;
  }

  .component-stage :global(.pf-icon),
  .text-link :global(.pf-icon) {
    height: 1rem;
    width: 1rem;
  }

  @media (max-width: 639px) {
    .component-index > div {
      padding-inline: 0;
    }

    .component-index > div + div {
      border-left: 0;
      border-top: 1px solid var(--dahlia-border-subtle);
    }
  }
</style>
