---
import { PageSection } from '@prosefly/astro-theme-dahlia/layouts';
import SponsorDirectory from './SponsorDirectory.astro';
import type { Sponsor, SponsorGroup } from '../../lib/sponsors';

interface Props {
  currentSponsorGroups: SponsorGroup[];
  oneTimeSponsors: Sponsor[];
  pastSponsors: Sponsor[];
}

const { currentSponsorGroups, oneTimeSponsors, pastSponsors } = Astro.props as Props;
---

<PageSection class="border-t border-(--dahlia-border-subtle) bg-(--dahlia-surface)/55" padding="lg" size="xl">
  <div class="mb-10 grid gap-8 lg:grid-cols-[minmax(0,0.58fr)_minmax(0,1fr)]">
    <div>
      <p class="section-label">Maintainer sponsors</p>
      <h2 class="section-heading mt-3">People and organizations supporting lepture.</h2>
    </div>
    <p class="max-w-2xl text-sm leading-6 text-(--dahlia-text-muted)">
      These sponsors support the maintainer's broader open-source work. Their
      support also helps make long-term maintenance for projects like Dahlia more
      sustainable.
    </p>
  </div>

  <SponsorDirectory
    currentSponsorGroups={currentSponsorGroups}
    oneTimeSponsors={oneTimeSponsors}
    pastSponsors={pastSponsors}
  />
</PageSection>

<style>
  .section-label {
    color: var(--dahlia-text-muted);
    font-family: var(--dahlia-font-mono);
    font-size: 0.8125rem;
  }

  .section-heading {
    color: var(--dahlia-text-strong);
    font-size: clamp(2rem, 3.6vw, 3.25rem);
    font-weight: 600;
    letter-spacing: -0.045em;
    line-height: 1.03;
  }
</style>
